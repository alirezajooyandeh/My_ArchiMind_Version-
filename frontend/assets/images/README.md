# Background Image

Place your background image file here with the name `background.jpg`.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`

The image will be used as a subtle background for the entire application.

